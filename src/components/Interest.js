const Interest=()=>{

   const data={
        title:"INTEREST",
        description:"Board Games,Photography, Cooking, Video Games, Sports and Exercise and of all Music."
    }

    return(
        <div>
            <h1>{data.title}</h1>
            <p>{data.description}</p>
        </div>
    );
}

export default Interest;